name: vm3 time finalizer delay

on:
  pull_request:

jobs:
  delay:
    name: vm3-time-finalizer-delay-7f31
    runs-on: ubuntu-latest
    steps:
      - name: Hold finalizer past wall-clock deadline
        run: sleep 240
