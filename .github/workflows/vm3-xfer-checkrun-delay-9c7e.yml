name: vm3 transfer checkrun delay 9c7e

on:
  pull_request:

jobs:
  delay:
    if: >-
      github.event.pull_request.title == 'VM3 transfer checkrun stale probe 9c7e' ||
      github.event.pull_request.title == 'VM3 transfer checkrun protected merge impact 9c7e'
    name: vm3-transfer-checkrun-delay-9c7e
    runs-on: ubuntu-latest
    steps:
      - name: Hold source provenance evaluation open
        run: sleep 600
