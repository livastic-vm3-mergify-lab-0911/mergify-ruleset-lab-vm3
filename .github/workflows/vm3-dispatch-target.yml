name: vm3 trusted dispatch target

on:
  workflow_dispatch:
    inputs:
      marker:
        description: controlled marker
        required: false
        type: string

permissions:
  contents: read

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - run: echo "vm3 dispatch marker=${{ inputs.marker }}"

