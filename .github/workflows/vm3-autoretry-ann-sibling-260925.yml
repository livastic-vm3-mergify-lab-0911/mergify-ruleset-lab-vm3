name: VM3 AutoRetry Annotation Binding 260925
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  sibling-annotate:
    name: sibling-annotate
    runs-on: ubuntu-latest
    steps:
      - name: Emit sibling-only annotation marker
        run: |
          echo "::error title=VM3 sibling annotation::VM3_ANN_BIND_260924"
          echo "sibling job itself succeeds"
  victim-fail:
    name: victim-fail
    runs-on: ubuntu-latest
    steps:
      - name: Fail without marker
        run: |
          echo "victim job fails without any retry marker"
          exit 1
