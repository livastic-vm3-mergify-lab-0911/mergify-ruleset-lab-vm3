commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-tmp-260920
queue_rules:
  - name: vm3-dupq-weak-260923
    queue_conditions:
      - base = vm3-dupq-target-260923
      - title = VM3 DUPQ SEQUENTIAL 260923
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-dupq-never-weak-260923
    merge_method: merge
  - name: vm3-dupq-strong-260923
    queue_conditions:
      - base = vm3-dupq-target-260923
      - title = VM3 DUPQ SEQUENTIAL 260923
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-dupq-never-strong-260923
    merge_method: merge
  - name: signature-mq
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-autogroup-guard
      - check-success = vm3-queue-name-hold-never
    merge_method: merge
merge_queue:
  mode: serial
  max_parallel_checks: 1
pull_request_rules:
  - name: vm3 queue-name revocation checkrun 9c7e
    conditions:
      - title = VM3 queue-name revocation checkrun 9c7e
      - queue-name = signature-mq
      - check-success = vm3-queue-name-delay-9c7e
    actions:
      comment:
        message: VM3_QUEUE_NAME_STALE_CHECKRUN_9C7E
