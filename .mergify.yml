queue_rules:
  - name: stale-permission-impact
    batch_size: 1
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-autogroup-guard
    merge_method: squash

merge_queue:
  mode: serial
  max_parallel_checks: 1
