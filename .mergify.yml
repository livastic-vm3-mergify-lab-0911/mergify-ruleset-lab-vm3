commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
scopes:
  source:
    files:
      old-area:
        include:
          - strict-a.txt
          - strict-b.txt
      docs-area:
        include:
          - vm3-taxonomy-docs/**/*
      config-area:
        include:
          - .mergify.yml
queue_rules:
  - name: vm3-taxonomy-tax933ca5
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-autogroup-guard
    merge_method: merge
    batch_size: 1
    max_checks_retries: 0
merge_queue:
  mode: serial
  max_parallel_checks: 1
  status_comments: all
