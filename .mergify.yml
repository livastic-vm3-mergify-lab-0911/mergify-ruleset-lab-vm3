commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-tmp-260920
queue_rules:
  - name: mixed-review-codeowner
    merge_conditions:
      - check-success = vm3-mixed-review-codeowner-never
    merge_method: merge
merge_queue:
  mode: serial
  max_parallel_checks: 1
