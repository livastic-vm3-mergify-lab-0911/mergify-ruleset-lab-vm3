commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911

queue_rules:
  - name: retarget-session
    batch_size: 1
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-autogroup-guard
      - check-success = vm3-retarget-hold
    merge_method: squash

merge_queue:
  mode: serial
  max_parallel_checks: 1
