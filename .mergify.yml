queue_rules:
  - name: branch-edit-guard
    queue_conditions: []
    merge_conditions:
      - check-success = vm3-queue-edit-guard
    merge_method: fast-forward
    allow_queue_branch_edit: false

merge_queue:
  mode: serial
  max_parallel_checks: 1
