pull_request_rules:
  - name: vm3 custom path autoqueue cp9f17
    conditions:
      - title = VM3 custom config path current E2E cp9f17
      - -mergify-configuration-changed
    actions:
      queue:

queue_rules:
  - name: cp9f17
    queue_conditions: []
    merge_conditions: []
    merge_method: merge

merge_queue:
  mode: serial
  max_parallel_checks: 1

scopes:
  source:
    files:
      critical:
        include:
          - critical/**
      docs:
        include:
          - docs/**
