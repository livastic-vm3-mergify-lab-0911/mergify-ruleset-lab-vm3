commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911

scopes:
  source:
    manual:

queue_rules:
  - name: vm3-scope-direct-merge
    batch_size: 1
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-autogroup-guard
    merge_method: merge

merge_queue:
  mode: parallel
  max_parallel_checks: 2
