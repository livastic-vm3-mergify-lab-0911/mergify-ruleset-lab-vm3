commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
queue_rules:
  - name: vm3-mrgfy8104-batch
    batch_size: 2
    batch_max_wait_time: 5 min
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-mrgfy8104-split-guard
    merge_method: merge
merge_queue:
  mode: serial
  max_parallel_checks: 1
