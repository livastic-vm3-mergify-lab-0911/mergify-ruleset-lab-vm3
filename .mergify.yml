commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-tmp-260920
queue_rules:
  - name: vm3-rpfresh-escstar-260920
    queue_conditions: []
    merge_conditions:
      - check-success = vm3-rpfresh-never-escstar-260920
    merge_method: merge
merge_queue:
  mode: serial
  max_parallel_checks: 1
pull_request_rules: []
