commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
scopes:
  source:
    files:
      "scope-ok\n::warning title=VM3_SCOPE_CMD_9C7E::scope-command-reached\n::set-output name=base::VM3_INJECTED_BASE_9C7E":
        include:
          - vm3-scope-probe.txt
queue_rules:
  - name: signature-mq
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-autogroup-guard
    merge_method: merge
merge_queue:
  mode: serial
  max_parallel_checks: 1
