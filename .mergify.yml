scopes:
  source:
    files:
      scope-x:
        include:
          - x/**/*
      scope-y:
        include:
          - y/**/*

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = vm3-direct-guard

merge_queue:
  mode: serial
