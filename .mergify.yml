commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
queue_rules:
  - name: signature-mq
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-autogroup-guard
    merge_method: merge
merge_queue:
  mode: serial
  max_parallel_checks: 1
pull_request_rules:
  - name: vm3 author permission freshness 9c7e
    conditions:
      - title = VM3 author permission freshness 9c7e
      - author-permission >= write
      - -draft
    actions:
      comment:
        message: VM3_AUTHOR_PERMISSION_FRESHNESS_9C7E
