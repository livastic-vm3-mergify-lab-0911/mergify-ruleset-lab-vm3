pull_request_rules:
  - name: vm3 trusted github actions positive
    conditions:
      - number = 14
      - label = documentation
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: vm3-dispatch-target.yml
              inputs:
                marker: trusted-positive

