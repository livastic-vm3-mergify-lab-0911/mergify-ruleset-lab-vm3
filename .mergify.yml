commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911

scopes:
  source:
    manual:

queue_rules:
  - name: vm3-scope-race-impact
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - label = vm3-never-scope-race-merge
    merge_method: merge

merge_queue:
  mode: parallel
  max_parallel_checks: 2
