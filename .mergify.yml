commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
queue_rules:
  - name: signature-mq
    queue_conditions:
      - base != vm3-merge-batch-review-0031cc
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - base != vm3-merge-batch-review-0031cc
      - check-success = vm3-autogroup-guard
      - check-success = vm3-queue-name-hold-never
    merge_method: merge
  - name: vm3-merge-batch-review-0031cc
    queue_conditions:
      - base = vm3-merge-batch-review-0031cc
    merge_conditions:
      - base = vm3-merge-batch-review-0031cc
      - label = vm3-merge-batch-go-0031cc
    branch_protection_injection_mode: queue
    batch_size: 2
    batch_max_wait_time: 2 minutes
    max_checks_retries: 0
    merge_method: merge-batch
merge_queue:
  mode: serial
  max_parallel_checks: 1
pull_request_rules:
  - name: vm3 queue-name revocation checkrun 9c7e
    conditions:
      - title = VM3 queue-name revocation checkrun 9c7e
      - queue-name = signature-mq
      - check-success = vm3-queue-name-delay-9c7e
    actions:
      comment:
        message: VM3_QUEUE_NAME_STALE_CHECKRUN_9C7E
