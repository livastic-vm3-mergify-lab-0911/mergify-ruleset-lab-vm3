commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
scopes:
  source:
    files:
      old-area:
        include:
          - vm3-taxonomy-area/**/*
      docs-area:
        include:
          - vm3-taxonomy-docs/**/*
queue_rules:
  - name: vm3-taxonomy-tx7d51f4
    queue_conditions:
      - check-success = vm3-taxonomy-guard-tx7d51f4
    merge_conditions:
      - check-success = vm3-taxonomy-guard-tx7d51f4
    merge_method: merge
    batch_size: 1
    max_checks_retries: 0
merge_queue:
  mode: serial
  max_parallel_checks: 1
  status_comments: all
