queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - label = vm3-stack-never-merge

merge_queue:
  mode: serial

commands_restrictions:
  queue:
    conditions:
      - or:
          - sender-permission >= write
          - sender = {{ author }}
