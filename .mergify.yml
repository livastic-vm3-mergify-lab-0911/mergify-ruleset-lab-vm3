pull_request_rules:
  - name: vm3 copy bot revocation probe
    conditions:
      - label = documentation
      - head ~= ^vm3-copy-(positive|after-revoke)$
    actions:
      copy:
        branches:
          - vm3-copy-target
        bot_account: livastic-vm3-secondary-20260911
