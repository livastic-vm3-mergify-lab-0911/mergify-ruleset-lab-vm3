commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911
scopes:
  source:
    files:
      "scope-ok\n::warning title=VM3_SCOPES_PRECEDENCE_9C7E::scope-command-reached\n::set-output name=scopes::{\"vm3-injected\":\"true\"}":
        include:
          - vm3-scopes-precedence.txt
queue_rules:
  - name: signature-mq
    queue_conditions:
      - check-success = vm3-autogroup-guard
    merge_conditions:
      - check-success = vm3-autogroup-guard
    merge_method: merge
merge_queue:
  mode: serial
  max_parallel_checks: 1
